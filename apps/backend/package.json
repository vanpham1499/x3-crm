{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "php -S 0.0.0.0:${PORT:-4000} -t public public/index.php",
    "start": "php -S 0.0.0.0:${PORT:-4000} -t public public/index.php",
    "build": "composer install --no-dev --optimize-autoloader",
    "db:migrate": "composer install && php artisan migrate",
    "db:seed": "composer install && php artisan db:seed",
    "db:fresh": "composer install && php artisan migrate:fresh --seed"
  }
}
