# Styles

- Global CSS entry: `@/styles/globals.css`.
- Component/page-specific CSS Modules can live next to the component as `*.module.css`.
- Shared CSS files should live here and be imported through `@styles/...`.
